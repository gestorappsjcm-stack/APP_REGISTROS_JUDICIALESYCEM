
# COMO CREAR ACCESO DIRECTO EN WINDOWS

-- 1. Vaciar tabla (mantener estructura)
TRUNCATE TABLE pac_datos_personales;

-- 2. Cargar nuevos datos (ejemplo con COPY o INSERT)
-- INSERT INTO pac_datos_personales (dni, apellidos_nombres, fecha_nacimiento, sexo)
-- VALUES (...);

-- 3. Verificar que RLS sigue activado (por defecto sí)
SELECT relrowsecurity FROM pg_class WHERE relname = 'pac_datos_personales';
-- Debe devolver: true
